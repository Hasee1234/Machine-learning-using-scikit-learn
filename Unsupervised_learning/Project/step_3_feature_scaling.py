#if we dont scale the data model will take large number as priority
# here will use logistic regression becuse for binary or 0,1 values we use it to ay pass or fail
#step 4= split the data
#step 5=select the model
#step 6=make predictions    
#step 7= evaluate model
#step 8= visualize results
#step 9=optional(improve/experiment)
#step 10=wrap up